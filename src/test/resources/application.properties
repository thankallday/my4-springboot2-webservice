
# p212 아래 내용이 없으면 lombok 을 이용힌 테스트 이외의 스프링을 이용한 테스트는 모두 실패한다.
# p213 에러 메시지 'No qualifying bean of type 'com.jojoldu.book.springboot.config.auth.Custom2OAuth2UserService''
#      이는 Custom2OAuth2UserService 를 생성하는 데 필요한 소셜 로그인 관련 설정 값들이 없기 때문이다.
#      이유는, main 과 test 환경의 차이로 둘은 본인만의 환경 구성을 가진다.
#            테스트시에는 src/test/application.properties 가 없으면 main 에 있는 설정을 그대로 가져온다.
#            다만,  application.properties 만 드대로 읽지,application-oauth.properties 는 해당되지 않아 그대로 가져오지 않는다
#            그래서 이 문제를 해결하기 위해 테스트 환경만을 위한 application.properties 를 만드는데 실제로 구글연동까지 테스트 하지 않으므로 가짜 설정값을 등록한다
# p214
spring.jpa.show_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.h2.console.enabled=true
spring.session.store-type=jdbc

# Test OAuth
spring.security.oauth2.client.registration.google.client-id=test
spring.security.oauth2.client.registration.google.client-secret=test
spring.security.oauth2.client.registration.google.scope=profile,email